query ArchivesRelations($id: String!) {
    relations(type: Archives, id: $id) {
        type
        total
        randomRelations {
            id
            label
            type
            relations {
                type
                total
            }
        }
    }
}
