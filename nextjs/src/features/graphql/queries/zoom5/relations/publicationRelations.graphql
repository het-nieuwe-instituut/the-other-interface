query PublicationRelations($id: String!) {
    relations(type: Publications, id: $id) {
        type
        total
        randomRelations {
            id
            label
            type
            relations {
                type
                total
            }
        }
    }
}
