query ObjectRelations($id: String!) {
    relations(type: Objects, id: $id) {
        type
        total
        randomRelations {
            id
            label
            type
            relations {
                type
                total
            }
        }
    }
}
