query stories($pagination: PaginationArg) {
    stories(pagination: $pagination) {
        data {
            id
            attributes {
                title
                slug
                description
            }
        }
        meta {
            pagination {
                page
            }
        }
    }
}
