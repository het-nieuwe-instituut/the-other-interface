query Zoom3DPublications($filterId: String!, $page: Int!, $pageSize: Int!) {
    zoomLevel3(entityName: Publications, filterId: $filterId, page: $page, pageSize: $pageSize) {
        uri
        name
        count
        total
    }
}
