query ZoomLevel3Publication($id: String!, $locale: String!) {
    zoomLevel3Publication(id: $id, locale: $locale) {
        id
        __typename
        title
        thumbnail
        description
    }
}
