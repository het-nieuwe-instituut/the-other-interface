query ArchiveRecordRelations($id: String!, $type: EntityNames!, $locale: Locale!, $page: Int!, $pageSize: Int!) {
    archivesRecordRelations(type: $type, id: $id, locale: $locale, page: $page, pageSize: $pageSize) {
        id
        titleR
        referenceNumber
        period
    }
}