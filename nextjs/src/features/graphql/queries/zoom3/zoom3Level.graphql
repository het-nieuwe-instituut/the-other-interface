query Zoom3Relations($type: EntityNames!  $id: String!, $lang: String!) {
    relations(type: $type, id: $id, lang: $lang) {
        total,
        page,
        randomRelations {
            title
			thumbnail
        }
    }
}
