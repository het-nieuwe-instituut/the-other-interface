query StoryHoverRecordRelations($id: String!) {
    storyWithoutRelations(id: $id) { 
        data {
            id
            attributes {
                title
                shortDescription
                author {
                    data {
                        id
                        attributes {
                            firstName
                            lastName
                        }
                    }
                }
            }
        }
    }
}
