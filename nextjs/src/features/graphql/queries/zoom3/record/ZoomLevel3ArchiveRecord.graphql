query ZoomLevel3ArchiveRecord($id: String!) {
    archivesRecordZoomLevel3(id: $id) {
        referenceCode
        titleType
        descriptionLevel
        size
        period
        access
        photographCreator
        rights
        permanentLink
        externalSource
    }
}